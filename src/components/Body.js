import React, {useState} from 'react'

function Body() {
    return (
        <div style={{display: "flex", flexWrap: "wrap", flexDirection: "column"}}>
            
        </div>
    )
}

export default Body
